# SideBell 決策紀錄

> 每則一行：日期 + 決策 + 一句理由。spec 檔（`SideBell_Spec_v0.5.md`）不在此修改，由開發者另行改版。
> 本檔只記錄 session 中新產生的決策；spec 內已載明的決策不重複抄錄。

## 2026-08-05

- **採用 Spectra SDD 流程管理本專案**：以 SpecDemo 為模板 bootstrap（`openspec/` + `.spectra.yaml`），讓跨 session 的規格與決策有單一真相來源。
- **`git init` 建立版控**：Spectra 的 snapshot / drift / commit 流程依賴 git，且比賽後需要 tag 與 GitHub Release。
- **Build 系統採 xcodegen（`project.yml`）**：Info.plist、Background Modes、Capabilities 等 BLE 關鍵設定以純文字管理，改動在 diff 上看得見，避免 pbxproj 被誤改而無聲失效。
- **Spectra `tdd: true`，但範圍限定核心邏輯**：wire format codec、重送/Ack 狀態機、去重邏輯 test-first；BLE / UI / AVFoundation / RevenueCat 的平台行為改以實機驗證清單把關，不寫低價值 mock。
- **Spectra `audit: true`**：本專案有三個安全表面（RevenueCat 金流、BLE 加密配對與 Ack 偽造、患者健康相關呼叫紀錄），且「靜默失敗」（呼叫未送出但患者端顯示成功）是本產品最致命的 bug 類型。
- **Deployment target 定為 iOS 18.0**：無任何 API 需求逼往上，取系統眼控門檻（iOS 18 + A12）對齊，語意上「能用眼控的裝置就一定裝得起 SideBell」；已知代價是 iOS 18–25 區間無實機可驗。
- **Universal app（iPhone + iPad 皆支援），患者端格子採 adaptive 欄數**：照顧者端必為隨身 iPhone；患者端眼控場景推薦 iPad（注視目標大、可架床邊固定距離），觸控/輪椅場景 iPhone 亦適用，故不做 iPad-only。
- **開發驗證機組合**：患者端 iPad mini 7（iOS 27 beta）、照顧者端 iPhone 15 Pro（iOS 26）。維持 beta 不降版，必要時另借正式版裝置。
- **實機驗證清單須標註 OS 版本**：凡「背景喚醒」與「State Restoration 復活」類結果若在 beta 機取得，須於正式版裝置複驗後才採信；功能性結果（配對、Ack 閉環、UI）不需複驗。
- **未 Ack 的呼叫狀態不持久化，只活在記憶體的 Delivery 狀態機**：呼叫本質有時效，App 重啟後復活的舊呼叫對照顧者是假警報，侵蝕產品唯一資產（信任）；改以縮短「未送達」紅色回饋的路徑來補償。
- **照顧者端啟用 `audio` background mode**：`isUrgent` 需「重複警報直到確認」，超出 BLE 喚醒的約 10 秒處理窗，不開等於緊急呼叫在鎖屏時只響一聲。與 spec 1.1 排除的「靜音音訊保活 hack」性質不同——此處是真的播放使用者需聽見的警報。附帶兩條實作紀律：(1) 非播放期間必須 deactivate AVAudioSession，不得長開；(2) 重複警報須有上限（如 2 分鐘自動停），不得無限響。
- **多患者（一 Central 對多 Peripheral）：架構預留、v1 UI 只做單患者**（議程第 6 項結案）：BLETransport 內部以集合管理連線、CallRecord 保留 `patientDeviceName`；UI/onboarding/設定頁一律只呈現一位患者。寫集合與寫 optional 成本差異極小，卻避免 1.1 重寫連線與重連策略。
- **簽章設定寫入 `project.yml`（`DEVELOPMENT_TEAM: VZWPMD258L`、Automatic）**：沿用 HerbMeet 專案的 Team ID。寫在 manifest 而非 Xcode UI，避免每次 `xcodegen generate` 後都要重新選 Team。
- **Bundle ID 定為 `com.shinrenpan.sidebell`**：spec 1 留待 App Store Connect 建立時定案的欄位，於 W1 建 `project.yml` 前先行拍板。
- **確認的來源未知時廣播給所有已連線患者端，不得直接拒絕**（W1 角色切換測試發現）：`callOrigins` 住在傳輸層、`stop()` 時清空，而呼叫清單住在 CallCenter 且將持久化到資料庫，兩者生命週期不同。「清單看得到、傳輸層不記得來源」是常態——照顧者端每次重啟 App 後皆如此。若直接拒絕，歷史清單裡的呼叫將永遠按不了「已收到」，患者端永遠停在「等待中」。廣播安全：患者端會靜默丟棄不認識的呼叫識別碼，且識別碼為隨機 UUID 不洩漏內容。
- **App 重啟後的狀態恢復須同時覆蓋還原路徑與手動啟動路徑**（W1 V9 驗證，實測 log 佐證）：兩個必要修正——(1) `didDiscoverCharacteristicsFor` 須檢查 `isNotifying` 以恢復訂閱旗標，因為還原時訂閱從未失效，`didUpdateNotificationStateFor` 不會再觸發；(2) `didDiscover` 發現「已連線但我方缺少特徵」的對端時須主動補做服務探索，涵蓋使用者手動開啟 App（無還原、但系統層 BLE 連線仍存活）的路徑。不補這兩處的症狀是「呼叫收得到（系統層訂閱未失效，delegate 一設上 notify 就進來），但顯示未連線且確認寫不出去」，患者端永遠停在「等待中」。
  - 更正：本條先前記為「`willRestoreState` 未被呼叫」，那是誤判——當時 Console.app 未開啟 info 訊息。實測 log 確認 App 被 BLE 事件喚醒時 `willRestoreState` **會**被呼叫；使用者手動開啟 App 時則不會，故兩條路徑都要覆蓋。
- **兩種「離線」必須分開處理，重連策略不同**（W1 連線循環的收尾決策）：(1)**範圍外斷線**——自動重連、永不放棄，這是照護產品的安全底線，照顧者走回房間時不能依賴他記得按按鈕（spec 3.1）；(2)**連上但探索不到服務**——真實意義是對端不是患者端角色，重試再多次也不會好，連續 3 次後停止自動重連。`[待正式 UI 實作]` 照顧者端須顯示顯眼提示「找不到患者端裝置，請確認對方已設定為患者端角色」與手動重新連線按鈕；不在丟棄式 PoC 畫面上做。
- **iPad 切換角色後仍持續廣播服務 UUID，根因未明**（W1 已知未解問題）：`stopAdvertising()` + `removeAllServices()` 已呼叫，保留 manager 不釋放也試過，對端仍被掃描到。實際影響有限——恢復時間 2.6 秒可接受，且此狀態只在角色設錯時出現，真實使用不會發生。以上述重試上限止血，不再追根因。若 1.1 有餘裕可重啟調查。
- **帶還原識別碼的 Bluetooth manager 一旦建立就不釋放**（W1 連線循環追查的最終根因）：`CBPeripheralManager` / `CBCentralManager` 帶 `RestoreIdentifierKey` 時，其狀態由系統保管——那正是 App 被終止後能復活的原因。把 manager 設為 nil 只丟掉我們這側的參考，系統層的廣播不會停止，且會打斷緊接在前的 `stopAdvertising`。實測後果：切換角色後 iPad 仍持續廣播服務 UUID，對端連上卻找不到服務，形成長達 16 秒、8 次的連線循環。改為 manager 長期持有、以意圖旗標（`wantsAdvertising` / `wantsScanning`）控制行為與連線狀態判定。此前三次修正（重啟掃描、主動連線、無服務斷線）都只在 central 端修補症狀，根因在 peripheral 端。
- **照顧者端訂閱成功後停止掃描，斷線時才重啟**（W1 log 噪音檢視發現）：連上後仍持續掃描唯一的用途是發現第二台患者裝置，而多患者介面 1.0 不做，等於讓照顧者手機整天為用不到的功能耗電，也違反 spec 3.1「維持長連線，不反覆掃描」。1.1 支援多患者時，把停止條件由「已有任一連線」改為「已達目標患者數」。
- **連上但探索不到 SideBell 服務時必須主動斷線，且不得掛連線意圖**（W1 重連速度量測發現）：留著這條無服務的連線，會讓 central 誤判自己「已連線」而忽略對端恢復後的廣播，一路卡到對端自行超時斷開——實測 66 秒。改為主動斷線後，重連時間降至約 4 秒。但實測進一步顯示：斷線後掛的連線意圖會在 1.2 秒內自行完成、又探索不到服務、又斷線，形成連線循環；對端若長時間不恢復服務，這個循環會整夜空轉耗電。因此這類斷線改為不掛連線意圖，改等對端重新廣播時由掃描發現。此為 audit 紀律中「靜默失敗」的典型案例。
- **藍牙不可用必須是獨立的連線狀態，且區分成因**（W1 V8/V10 驗證中發現）：藍牙關閉時原本仍顯示「掃描中」，等於謊稱系統正在運作——照顧者誤觸控制中心關掉藍牙後，會以為呼叫收得到，實際上完全失聯。與先前「顯示已連線卻送不到」同屬假安全感。新增 `ConnectionState.unavailable(BluetoothUnavailability)`，區分 poweredOff / unauthorized / unsupported，因為使用者要採取的行動不同。`unknown` / `resetting` 屬啟動暫態，不映射，以免文字閃爍。`[待正式 UI 實作]` 正式畫面需以顯眼樣式呈現並引導使用者開啟藍牙或授權。
- **連線狀態以「已訂閱呼叫通道」判定，不以 BLE link 層的 connected 判定**（W1 V5 驗證中發現）：BLE 的 link 與 GATT 服務是兩層，對端釋放服務後 link 可能仍在，此時呼叫送不到卻仍顯示已連線。spec 2.1 的綠燈語意必須是「呼叫真的送得到」——顯示已連線卻收不到，是比顯示未連線更危險的假安全感。同時補上 `didModifyServices` 回呼處理服務失效。
- **BLE 配對採系統預設的 Just Works，不自建應用層金鑰交換**（W1 實機驗證後確認）：Core Bluetooth 未提供 API 指定 IO capability，自訂 GATT 服務無法要求配對碼。Just Works 提供 link-layer 加密（擋被動竊聽）與未配對裝置的 GATT 層拒絕（擋偽造 Ack），但不抗「首次配對當下在場」的主動中間人。威脅模型下可接受：攻擊者須物理在場於設定當下，收益僅為偽造一則呼叫。對外文案僅可宣稱「不可被鄰近裝置竊聽／偽造」，不得出現「端對端加密」或「防中間人」。
- **傳輸事件的消費者壽命與 App 相同，停止傳輸時不停止消費**（W1 實機測試中發現的 bug）：`AsyncStream` 只能迭代一次，取消消費 Task 會使序列永久終止，重新啟動接到的是死序列。症狀為「切換角色後再也連不上，重開 App 才正常」。已由 `CallCenterLifecycleTests` 釘住。
- **患者裝置名稱直接採用照顧者端的 `CBPeripheral.name`，Device Info 特徵改為選用的自訂暱稱**（W1 V9 驗證中修正）：`CBPeripheral.name` 取得的是系統設定裡的裝置名稱（如「Joe iPad mini 7」），spec 3.2 期望的「阿公的 iPad」照顧者端本來就看得到，1.0 不需要為此做設定頁欄位。Device Info 特徵的語意改為「患者自訂暱稱」，未設定時傳空值，照顧者端據此保留 `CBPeripheral.name`。
  - 更正：本條先前記為「必須做 App 內自訂暱稱，因為 `UIDevice.current.name` 自 iOS 16 起只回機型名稱」。前半是誤判——`UIDevice.current.name` 的限制屬實，但那不是唯一來源。實測中「來源」欄位先顯示完整名稱、讀到 Device Info 後才變成「iPad」，正是我用較差的值覆蓋了較好的值。
- **進入點採 AppDelegate + SceneDelegate（UIKit lifecycle）託管 SwiftUI**：符合既有 MVVMC 慣例，且 Core Bluetooth State Restoration 需要在 `application(_:didFinishLaunchingWithOptions:)` 早期重建 manager，SwiftUI `@main` App 生命週期較難保證此時序。

## 2026-08-06（W2 入口架構討論）

- **App 最外圍為三層容器**：首頁（兩顆角色按鈕，包在 `UINavigationController` 內——`AppRouter` 依賴 `source.navigationController`）→ 選擇角色後 fullScreen present 對應容器；患者端為 `UINavigationController`、照顧者端為 `UITabBarController`（內含兩個 `UINavigationController`，因兩個 Tab 都需要 push 子頁）。角色是「模式」而非導航層級，故用 present 而非 push。
- **已選角色時啟動立即 present 角色容器且不帶動畫，首頁仍為 root**：照顧者每次開 App 都要重選身分是災難場景（凌晨警報響、解鎖後還要先選身分）。首頁不可真的跳過建立，否則設定頁「切換角色」時 dismiss 沒有目標。
- **藍牙不可用時停用角色按鈕，但 `unknown` / `resetting` 一律放行**：首次啟動時尚未建立任何 `CBManager`（沒選角色就不會建），藍牙狀態為 `unknown`；若一併停用會死鎖——權限對話框要等 manager 建立才會跳，而 manager 要選了角色才會建。
- **免責聲明永久呈現於首頁，首次啟動須勾選「我已了解」才啟用角色按鈕**：永久呈現讓換照顧者、隔數月再開的人都看得到；首次的主動確認是產品責任考量——「使用者曾主動確認」與「聲明有印在畫面上」在延誤 119 的情境下重量完全不同。同頁完成，不增加 onboarding 流程。
- **患者端不設 Tab／Segment，格子佔滿全螢幕，設定入口為右上角小按鈕且須長按**：Tab 會吃掉底部面積，而眼控精度容錯與格子實體尺寸直接相關；更嚴重的是誤觸——患者一旦離開呼叫畫面可能無法自行返回，等同系統當場失效。改用「長按」而非尺寸來區隔：系統 Dwell Control 觸發的是單擊，不產生長按，故眼控天然無法觸發。`[待實測確認]` Dwell Control 是否確實只產生單擊。
- **照顧者端 Tab 1 = 連線狀態 + 進行中呼叫 + 歷史紀錄；Tab 2 = 設定（角色切換、警報、支持開發者、免責、關於）**：「進行中」與「歷史」分層呈現，因為警報響時照顧者要的是「按哪裡確認」，不是捲清單找最新一則。贊助入口依 spec 5 僅得出現於此。
- **不做獨立的全螢幕警報畫面**：半夜真正被看到的介面是鎖屏通知，不是 App 畫面；全螢幕接管只在「App 已開著」或「剛從通知進入」時有意義，而後者只要「進行中呼叫」區塊夠突出即可涵蓋。另一理由是風險——全螢幕接管需在「剛被背景喚醒」的狀態下 present，而 W1 的十個缺陷有六個都出在非正常路徑上。回頭改的觸發條件：加強「進行中」區塊到極限仍不足以讓半夜的照顧者一眼辨識時。

## 2026-08-07（無障礙範圍討論）

- **無障礙隨畫面實作，不留到 W5**：spec 2.1 要求 VoiceOver / Switch Control / Dynamic Type，眼控只是操作方式之一。留到最後補做等於重寫——自訂控制項 VoiceOver 讀不到、朗讀順序與視覺順序不一致、狀態變化沒有通知；隨畫面做幾乎零額外成本。spec 第 7 節的 W5「無障礙打磨」是實測與調整，不是從零開始。**每個 change 的驗收條件都須包含該畫面的 VoiceOver 可操作性。**
- **患者端的閉環回饋不得只有視覺形式**（W2 無障礙討論中發現的缺口）：spec 2.2 的「✓ 打勾」是患者唯一的安心來源，但純視覺對視障患者等於不存在。Ack 到達時須另有聲音或震動回饋。屬患者端格子畫面的 change，此處先記錄以免遺漏。
- **目標族群包含視障者，操作路徑分兩條**：視障但手能動 → VoiceOver + 觸控；視障且手不能動 → Switch Control（頭部／呼吸／眨眼等單一開關掃描）搭配聽覺回饋。眼控僅適用於視力正常且無法言語或行動者。
- **「停止自動重連」的計數必須依斷線原因區分，對端消失時要清除**（W2 E6 驗證發現，屬 W1 修正引入的迴歸）：W1 為阻止「連上卻拿不到服務」的空轉而加了「連續 3 次停止重連」，但它分不出兩種失敗——(1) 對端不是患者端角色（持續廣播，重試無用，該停）；(2) 對端消失又回來（裝置關機、App 被終止、走出範圍，必須重連）。iOS 在對端主動消失時會於 `didDisconnect` 帶錯誤，我方主動斷線則不帶，據此區分。未修正的症狀是「患者端裝置重開機後，照顧者端永遠連不上，除非自行重開 App」——而照顧者不會知道要這麼做。
- **不信任系統交還的 `isAdvertising` / `isScanning`，一律先停再開**（W2 覆蓋安裝時發現）：Xcode 覆蓋安裝後，系統會把上一版 App 的 BLE 狀態交還給新版，其中包含「正在廣播／掃描」——但那屬於已被替換的實例，實際上不會被任何對端看到。信任該旗標會跳過啟動，症狀是「兩端都顯示運作中，卻永遠連不上」。代價僅數毫秒。
  - `[待 W6 確認]` 真實使用者從 App Store 更新時是否有同樣現象。開發期間的緩解方式是**每次改動後刪除 App 再安裝**，而非覆蓋安裝。
- **重連在達到失敗上限後只降頻、不放棄**（W2 E6 驗證中修正）：原本「連續 3 次無服務即停止自動重連」實測在五秒內就用完額度，而使用者重開一次 App 遠不止五秒——結果是患者端重開機後照顧者端永遠連不上。改為冷卻 10 秒後重新給機會。「只要對端回來就必須連上」是 spec 3.1 的安全底線，不得為省電而放棄；空轉問題以降頻解決即可。
- **不引導使用者以「回首頁重選角色」來加速重連；改由正式 UI 的「重新連線」按鈕提供**（W2 E6 測試中提出）：重選角色會重建端點並清除所有重試狀態，因而能立即連上，但那是副作用而非設計意圖。照護場景中沒有人會盯著連線狀態，等照顧者發現異常才動手，早已錯過該收到呼叫的時間——能靠使用者主動操作解決的問題，在本產品等於沒解決。`[待正式 UI 實作]` 的「重新連線」按鈕內部行為即為重置傳輸狀態，但語意單一明確。
- **測試情境與真實情境對延遲的感受差距極大**（W2 觀察）：18 秒的重連對「盯著螢幕的測試者」很煎熬，對「手機放口袋的照顧者」無感。評估延遲是否可接受時，須以真實使用姿態為準，而非測試時的體感。
- **開發語言（fallback）設為 `en` 而非 `zh-Hant`**（W2 多語系討論）：development language 是裝置語言不在支援清單時的 fallback。Shipaton 評審多半使用英文裝置，而 Peace Prize 評的是產品使命——他們得先看懂才能評。現在改的成本近乎零（純設定，目前只有一份字串故行為不變）；等字串寫完再改則需重新歸類所有本地化資源。字串本地化本身仍屬 W4，1.0 支援繁中／英，`[v1.1]` 加印尼語與越南語（spec 9.4）。
- **App 須向系統宣告支援的語言，否則 VoiceOver 會用錯語音**（W2 A1 測試中發現，屬前一條決策的副作用）：把 development language 改為 `en` 之後，App 對系統宣告自己是英文 App，但未提供任何中文本地化資源——VoiceOver 因而以**英文語音**朗讀中文內容。實測後果包含「119」被唸成英文數值。修法是加入 `en.lproj` 與 `zh-Hant.lproj`（內容可為空註解），讓系統知道 App 支援哪些語言。字串本地化本身仍屬 W4。
- **緊急電話號碼須提供專供朗讀的寫法**（W2 A1 測試中發現）：顯示文字寫「119」，`accessibilityLabel` 寫「一一九」。朗讀「119」會被當成數值唸出，而那是緊急電話——唸錯會讓視障使用者撥錯號碼。此為安全問題而非體驗問題。**任何未來新增的電話號碼、金額、代碼都適用同一原則。**
- **W4 的本地化必須涵蓋 App 內字串，不能只翻 App Store 描述**（W2 A3 測試中觀察）：在英文系統的 iPad 上開啟 VoiceOver，中文內容被**韓文語音**朗讀——語言標記不明確時，語音引擎會從已安裝語音中任選一個能處理 CJK 的，選擇不可預測。無障礙是 Peace Prize 的評分項目，評審若以英文裝置搭配 VoiceOver 測試即會遇到此現象。完成英文本地化後問題自然消失（英文系統顯示英文字串、使用英文語音）。
- **患者端設定入口由「長按」改為「兩步驟單擊確認」**（W2 A3 驗證失敗後改設計）：原設計以長按擋眼控誤觸（Dwell Control 只發單擊），實測確實擋得住眼控，但也擋住了 VoiceOver 使用者——「雙擊並按住」時機難以拿捏，改用無障礙自訂動作亦不穩定。**可達性不等於可用性**：需要重試的操作，對每天使用的人就是障礙。改為連續兩次點擊、中間控制項外觀與文字改變、三秒未完成自動取消。防護不再依賴「某種使用者做不出某個手勢」，而依賴「兩次精準命中且目標會變」——對所有輔助技術一視同仁。進入待確認狀態須主動朗讀通知，否則視障使用者會以為第一次點擊無效。
- **入口控制項採純文字而非圖示**（W2 使用者提議）：照顧者一輩子只按幾次，需求是「第一次就找得到」，而圖示要先看懂才知道是什麼。字級與顏色保持低調，避免把患者注意力從格子上引開。
- **患者端設定入口屬於主畫面的 toolbar，不屬於容器**（W2 架構檢討）：先前把 UIButton 疊在 `UINavigationController` 的 view 上，繞過整個 MVVMC 分層——狀態管理落在容器、導航直接呼叫 AppRouter、按鈕與內容搶同一塊空間（VoiceOver 聚焦框會把它與下方內容框在一起）。改為主畫面 toolbar item：狀態歸該畫面 ViewModel、導航走 `onRoute`。代價是頂部多佔約 44pt，但省下那 44pt 換來架構偏離與無障礙缺陷不划算。兩步驟確認抽成可重用的 `TwoStepConfirmButton`，格子畫面取代驗證畫面時直接沿用。
- **患者端設定的防護目標是「無意誤觸」，不是「刻意也打不開」**（W2 討論釐清）：該入口的實際操作者是照顧者，他必須打得開；因此「刻意嘗試能開」是正確行為。就算患者出於好奇打開，三秒逾時與照顧者可代為退出即可收拾；真正的災難是「無意間離開呼叫畫面且自己回不來」。此釐清同時說明按鈕為何可以做得明顯——防護靠兩步驟，不靠把目標做小。
- **保留 VoiceOver 可達性的理由是視障照顧者，而非患者**（W2 討論釐清）：照顧者本人可能是視障者（家屬照顧家屬，照顧的一方也可能有障礙）；一旦他無法進入設定，就無法為患者調整格子內容，整套系統對該家庭失效。這是不能回頭採用長按的原因——長按擋掉的不只是誤觸，還有一整類使用者。
- **患者端保留導覽列，並於左側承載連線狀態指示**（W2 版面決策）：導覽列佔頂部約 44pt（iPad mini 直向約 4%、橫向約 6%），但那不是純損失——spec 2.1 要求患者端頂部有常駐的連線狀態指示（綠＝已有照顧者連線／紅＝無），而該指示必須固定、顯眼、不隨格子捲動。沒有導覽列就得自製一條頂部列，還要自行處理 safe area、Dynamic Type 與 VoiceOver 分組，屬重複造輪子。版面定為：導覽列左側放連線指示、右側放設定入口，其餘全給格子。此決定直接影響患者端格子畫面的版面，於該 change 開始前先行定案。
- **App Store Connect 的 App 記錄已於 2026-08-08 建立，名稱 `SideBell` 佔位完成**：主要語言選「英文（美國）」，與開發端的 development language 一致，理由相同——Shipaton 評審多以英文環境檢視。套件識別碼 `com.shinrenpan.sidebell`、SKU 同值。**提前佔名的決定性理由是本專案已因名稱衝突改名過一次**（EyeTalk 因 Google Play 同名 App 棄用，spec v0.3），而 spec 1 要求所有識別字、GATT 命名、commit message 統一——再撞一次名，要改的是整個 codebase、兩份驗證清單與全部決策紀錄。台灣使用者看到的中文名稱之後以本地化形式加上（App 資訊 → 本地化 → 繁體中文 → 「SideBell 隨身鈴」），屬 W4／W6 範圍。

## 2026-08-08（患者端設計原則）

- **SideBell 是「多一個選項」，不是必用工具——患者端一律取簡**（產品哲學，適用於後續所有患者端設計）：目標族群的每一次互動都有實質成本（眼控需凝視數秒、單指操作費力），因此任何功能都必須先通過「這對患者有行動價值嗎」這一關。沒有行動價值的資訊與控制項一律不做，即使它們「比較完整」。簡單優先於完備。
- **患者端不提供「取消呼叫」功能**：誤觸的後果是照顧者白跑一趟；但取消按鈕本身是新的誤觸來源，**患者可能誤觸取消掉真正的呼叫**，導致他以為求助已送出而實際上無人前來——後者嚴重得多。取消功能守護的是較輕的損失，卻引入較重的風險。
- **患者端的呼叫狀態只有三種：等待中／✓ 已確認／✗ 未送達**：不區分「尚未送出」與「已送達但未確認」——該區分對患者沒有行動價值（他無法改變與照顧者的距離）。只有「未送達」具行動價值：告訴他這條路不通，該改用其他方式求助。重送的次數與間隔等機制細節一律不呈現。

## 2026-08-08（議程第 2 項：重送／Ack 狀態機，結案）

- **需要自動重送，患者只按一次**：目標族群的每次操作都有實質成本（眼控需凝視、單指操作費力），若不自動重試，等於把輪詢外包給最沒有能力做這件事的人。照顧者暫時離開範圍是每天都會發生的情境，不該讓它等同於呼叫失敗。
- **重送採事件驅動，不做定時輪詢**（修正 spec 3.4 的「每 10 秒重送」）：送不出去即放入待送佇列，於**連線恢復事件**觸發時立即送出。比定時輪詢更快（連上即送，無需等待下一個週期）也更省電，並與 spec 3.1 已定的「不自行輪詢，靠系統事件」一致。
- **逾時設為 3 分鐘**（修正 spec 3.4 的「最多 6 次 ≈ 1 分鐘」）：判準不是技術上的重試成本，而是「等多久才算耽誤了患者」。一分鐘太短——照顧者去趟廁所就會被判失敗；三分鐘則足以涵蓋短暫離開，而超過三分鐘無人回應時，患者確實該考慮其他求助方式。
- **最終失敗狀態的文字用「無人回應」而非「未送達」**：有一種情況「未送達」是錯的——呼叫確實送達照顧者手機、警報也響了，但他在睡覺未按確認。「無人回應」在兩種情況下都為真，且對患者的行動含義相同（沒人來，該換方式）。
- **多照顧者情境沿用 spec 3.4 原文**：一 Peripheral 可被多 Central 同時訂閱，任一人 Ack 即全體閉環，不需另行設計。

## 2026-08-08（議程第 3 項：患者端防休眠，結案）

- **患者端容器啟用 `isIdleTimerDisabled`，離開角色時關閉**：螢幕保持開啟不是偏好而是必要條件——裝置鎖屏後純眼控的患者**無法喚醒它**（眼球追蹤於鎖定畫面不運作），螢幕一暗整套呼叫系統即等同離線，而患者沒有任何辦法讓它回來。照顧者端不啟用，其手機須正常鎖屏。此決定使 spec 3.6.3 的「插電」從建議變成必須。
- **引導使用模式只能教學，無法由 App 啟用**：Guided Access 需使用者手動開啟並連按三下側邊按鈕，App 無 API 可代勞。連同「不要滑掉 App」「調低亮度」一併納入 onboarding 需求（後續 change）。

## 2026-08-08（W3 實作）

- **`ModelContext` 不持有 `ModelContainer`，容器必須由長壽物件持有**（W3 實作中踩到）：把容器放在區域變數、只把 `mainContext` 傳出去，函式返回後容器即被回收，留下指向已死容器的 context——下次操作直接進 SwiftData 的斷言（`EXC_BREAKPOINT`，**不帶任何訊息**，測試會不斷 crash 重啟而看不出原因）。正式程式碼中容器由 `AppDelegate` 持有；測試中由 suite 的 stored property 持有。診斷方式：寫四個逐步縮小的測試（建容器／建模型／插入／儲存），全部通過就代表問題在呼叫端的生命週期而非 SwiftData 用法。
- **`@Model` 型別必須標 `nonisolated`**：本模組預設 MainActor 隔離（`SWIFT_DEFAULT_ACTOR_ISOLATION`），而 SwiftData 會在自己的 context 上操作模型物件。
- **使用 `mainContext` 的測試須標 `@MainActor`**：Swift Testing 會把測試包進 `@Sendable` closure 執行，不標的話隔離推斷與執行期不一致。

## 2026-08-08（W3 雙機驗證後的修正）

- **格子不設圖示，識別完全交給文字**（適用於後續所有格子相關設計）：照顧者將來要能自行編輯格子，而圖示無法被編輯出來——從幾千個 SF Symbols 裡挑對照顧者是不合理的負擔，改給小圖庫則必然涵蓋不到真實需求（「換尿布」「抽痰」）。兩條路的結果都是預設格子有圖、自訂格子沒圖的不一致。標題本身還多一層：預設項目走翻譯，自訂項目是照顧者打的原文。因此拿掉 `symbolName`，標題字級放大到能在掃視中辨認，長標題以縮放而非截斷處理（截掉的字尾可能正是兩格的唯一差異）。**注意這與「狀態以符號與文字表達」不衝突**——那條講的是呼叫狀態的符號，不是格子項目的圖示。
- **傳輸事件必須扇出給每個消費者，`AsyncStream` 不是多播的**（雙機實測中踩到）：`CallCenter` 與 `CallDelivery` 兩個與 App 同壽命的消費者曾迭代同一條序列，結果是事件被瓜分——每則只送到其中一個。症狀極具欺騙性：呼叫與確認「一則到、一則不到」呈交替出現，兩端都沒有錯誤可看，連線指示還會停在錯誤的狀態。修法是把契約從 `var events` 改為 `func makeEventStream()`，讓「共用同一條」在型別上就不可能。**單元測試抓不到這類 bug**，因為每個測試只有一個消費者。
- **並列的時間戳不能拿來排序**（同上）：待送佇列存在字典裡，字典迭代順序不確定、`sorted` 也不保證穩定，因此 `triggeredAt` 並列的呼叫會以隨機順序送出——患者連按兩格後照顧者回到範圍，收到的順序可能是倒的。改用單調遞增的觸發序號。這個 bug 讓測試變成擲骰子，曾經矇混通過一輪。

## 2026-08-08（眼控實測後的兩項約束，尚未實作）

以下兩條在 W3 雙機驗證中以眼控實際操作後發現，**均不在 W3 範圍內**，是格子編輯（W4）必須遵守的約束。

- **格子數量以「一屏放得下」為上限；捲動是降級路徑，不是主要方案**（W4 硬約束）：

  **不是因為捲動做不到**——滑動手勢與 AssistiveTouch 的捲動選單對眼控患者確實不可用（Dwell Control 只發出單擊），但 App 自己畫兩顆大的上下箭頭按鈕，眼控是點得動的。真正的理由是**緊急呼叫必須零捲動可達**：患者按「不舒服」的那一刻是整個產品摩擦成本最不能容忍的時刻，若那一格在第二屏，他要凝視箭頭數秒、等畫面捲動、重新視覺搜尋、再凝視目標數秒——而眼控的注視穩定度會隨身體狀況下降，身體狀況差正是他要按那一格的原因。

  另有兩項次要但真實的代價：（1）箭頭與右側格子競爭同一片注視區域，要避開就得從格子身上割出間距，格子變窄又反過來降低眼控精度；（2）捲動位置是一種狀態，而患者記住的是「喝水在左上角」這類**實體位置**——這與既有的「順序必須穩定」是同一條原則，捲動會破壞它。

  因此：

  1. **主線**——格子數上限 = 螢幕高度 ÷ 最小格子尺寸（短邊 150pt），在編輯畫面**擋在「新增」那一步**。此外**建議上限 6 格**：外部無障礙指南以「視覺疲勞導致眼球控制力下滑」為由建議單頁 4-6 個按鈕，這是純幾何公式看不到的維度。該數字未附實證來源，因此列為建議而非硬上限——硬上限仍是「一屏放得下」，但編輯畫面超過 6 格時應提示照顧者。訊息不是「不能再加」，而是「再加就會有格子患者看不到，要不要換掉現有的某一格」，逼照顧者做取捨——取捨正是這個產品該逼他做的事。這也與「患者端一律取簡」一致：每多一格，患者每次尋找目標的視覺掃描成本都會上升。
  2. **降級**——真的無法收斂到一屏的家庭（需求特別多樣的患者），才提供上下箭頭捲動。此時**緊急格子必須釘在第一屏**，不隨捲動移出視野。

  `design.md` 原本寫的「屆時需要捲動或分頁」作為主要解法是錯的，已作廢；捲動保留為降級路徑。
- **不自行實作 dwell 冷卻時間，重複觸發用狀態去重**：外部無障礙指南建議在 Button action 內加入 2 秒冷卻，理由是「眼球肌肉在觸發後無法立刻移開視線」。**該問題在 iOS 的 Dwell Control 已由系統處理**（觸發後需視線離開再回來），2026-08-08 的眼控實測也確認不會連發。同一份指南另有一條原則說得對——「不要在 App 內部自己用 Timer 計算 2 秒或 3 秒，把時間控制權交給使用者」——而 2 秒冷卻正是它自己反對的那種硬編值。我們真正要解的是下面這個不同的問題。
- **同一格重複觸發用狀態去重，不用時間節流**：患者會因為不確定而重按（「我剛剛按到了嗎」）。時間節流（例如兩秒內不准再按）是拍腦袋的門檻，且會在邊界產生「我凝視了好幾秒卻什麼都沒發生」——對每次觸發都有實質成本的族群，零回饋是最糟的結果。正解是：該格**目前有「等待中」的呼叫時，重按不產生新呼叫，但語音與音效照樣重播一次**（語意是「你的呼叫還在等，我再確認一次給你聽」）；該格若為「已確認」「無人回應」或無狀態，重按則正常產生新呼叫——G8 的驗證因此不受影響。這條規則不需要任何魔術數字，直接從「一格只顯示最近一則呼叫」推導而來。

