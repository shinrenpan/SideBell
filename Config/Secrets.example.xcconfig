// SideBell — 建置時注入的組態範本。
//
// 複製為 `Config/Secrets.xcconfig`（該檔已列入 .gitignore）並填入實際值。
// 缺少該檔時建置仍會成功，但 RevenueCat 不會被初始化，贊助頁會顯示載入失敗
// ——那是刻意的：金流設定缺失不該讓整個 App 起不來，呼叫功能與它無關。

// RevenueCat 的 **Public SDK Key**（`appl_` 開頭）。
// 這把 key 本來就設計成隨 App 出貨、可被反編譯取得，不是機密憑證；
// 不放進版控只是不想讓它出現在公開 repo 的搜尋結果裡。
// 真正需要保密的是 Secret Key（`sk_` 開頭）——那一把**絕不可**進入 App。
// 測試期間可改用 **Test Store** 的 key（`test_` 開頭）：購買流程改由 SDK 自己
// 彈系統 alert，提供「購買／失敗／取消」三個結果，完全不碰 App Store——
// 不需要沙盒帳號，商品也不必先過審。Release build 誤用時 SDK 會自己擋下。
// key 於 RevenueCat 主控台的 Test Store 專案取得。
REVENUECAT_API_KEY = appl_YOUR_KEY_HERE
